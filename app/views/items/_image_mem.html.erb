<%= form_for [:jar, @item] do |f| %>
  <div class="field">
    <%= f.label :type_data, 'Upload a video or image' %>
    <%= f.file_field :type_data, id: 'fileupload' %>
  </div>
  <div class="comment-field">
    <%= f.label :comment %>
    <%= f.text_field :comment %>
  </div>
  <div class="action">
    <%= f.submit "Submit Video", id: 'submit-new-mem' %>
  </div>
<% end %>
