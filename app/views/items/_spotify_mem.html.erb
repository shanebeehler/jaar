<%= form_for [:jar, @item] do |f| %>
  <div class="comment-field">
    <%= f.label :comment %>
    <%= f.text_field :comment %>
  </div>
  <div class="action">
    <%= f.submit "Submit Video", id: 'submit-new-mem' %>
  </div>
<% end %>
